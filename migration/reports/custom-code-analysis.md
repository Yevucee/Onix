# Custom Code Analysis

**Sources:** Astra theme zip, WordPress XML export, database dump, `CFO ROI Calculator.html`

---

## Theme analysis

### Active theme: Astra (stock)

| Item | Finding |
|------|---------|
| Child theme | **Not present** |
| Custom PHP in theme | **None** — standard Astra `functions.php` only |
| Custom theme templates | **None** |
| Astra version in zip | 4.x (July 2025 files) |
| Live site Astra version | 4.13.8 (from crawl) |

**Classification:** **Obsolete** — Astra will not be used in new stack. Design tokens extracted to `docs/design-system-source.md`.

### Astra Customizer overrides (from live site CSS)

- Link colour: `#ff0000` / hover `#f00000`
- Button colour: `#ec0223`
- Logo width: 110px
- Container max-width: 1240px
- Font: system sans-serif stack at 15px base

**Classification:** **Migrate** (as design tokens, not Astra config)

---

## Custom CSS

### 1. Elementor per-widget custom CSS (sticky header)

Embedded in `_elementor_data` on header templates:

- Sticky header white background on scroll
- Logo transition and resize
- Navigation link colour states

**Classification:** **Migrate** → Header component CSS in new site

### 2. WordPress `custom_css` post

One `custom_css` post type in export (additional global styles).

**Classification:** **Migrate** — review contents during header rebuild

### 3. Astra inline CSS

Generated by Customizer — colours, typography, layout widths.

**Classification:** **Replace** — superseded by new design system

---

## Custom JavaScript

### 1. CFO ROI Calculator (`migration/source/custom-code/CFO ROI Calculator.html`)

Self-contained calculator with:

- Scoped CSS variables (Onix brand colours)
- Input sliders and calculated outputs
- Fonts: Inter, Space Grotesk
- ID scope: `#onix-roi-calc`

**Classification:** **Migrate** → React component on `/cfo-roi/`

### 2. Elementor HTML widgets

19 `html` widget instances across Elementor content — may contain inline scripts.

**Classification:** **Needs review** — audit each during page migration

### 3. Third-party plugin JS (not in repo)

Active plugins with frontend JS:

| Plugin | Classification |
|--------|----------------|
| Google Site Kit | **Replace** — new GA4/GTM integration |
| Jetpack | **Obsolete** — not needed |
| Complianz GDPR (DB tables) | **Replace** — new consent solution |
| Cookie Law Info (DB tables) | **Replace** — consolidate to one consent tool |
| Floating Button / Sticky Button | **Replace** — custom CTA component |
| Forminator | **Replace** — new form handling |
| FluentForm | **Replace** — new form handling |
| Contact Form 7 | **Obsolete** — superseded by Forminator/FluentForm |
| Pretty Links | **Replace** — Payload redirects collection or middleware |
| Redirection plugin | **Migrate** — rules to redirects collection |

---

## Custom PHP / hooks / shortcodes

No custom PHP plugins or theme functions found in supplied source material.

Shortcode usage: 2 instances of `shortcode` widget in Elementor data.

**Classification:** **Needs review** — identify shortcodes during content extraction

---

## Custom post types

| CPT | In export | Classification |
|-----|-----------|----------------|
| `elementor_library` | 46 | **Obsolete** — templates become React components |
| `elementor-hf` | 1 | **Obsolete** |
| `forminator_forms` | 2 | **Replace** — Payload form or external service |
| `wpcf7_contact_form` | 1 | **Obsolete** |
| `custom_css` | 1 | **Migrate** (styles only) |

No custom CPTs for Leadership, Data Centres, etc. — leadership content is stored as regular Pages.

**Classification for leadership pages:** **Replace** — move to `Leadership` collection

---

## Custom fields

No ACF or custom field plugin detected in active plugins list.

Metadata is Elementor postmeta and Yoast SEO fields.

**Classification:** N/A — use Payload field definitions

---

## Form integrations

| Form | Plugin | Location | Classification |
|------|--------|----------|----------------|
| Contact form(s) | Forminator (2 forms in export) | `/contact-us/`, possibly others | **Replace** |
| Contact form | Contact Form 7 (1 form) | Legacy | **Obsolete** |
| FluentForm | DB tables present | Unknown pages | **Replace** |
| Elementor Pro forms | 6 form widgets | Various pages | **Replace** |
| DB7 Forms | `wp_db7_forms` table | Legacy contact DB | **Obsolete** |
| Elfsight contact form | DB tables | Widget embed | **Replace** |

**Migration requirement:** New contact form with email notification, spam protection, conversion tracking.

---

## Tracking / analytics code

| Service | Evidence | Classification |
|---------|----------|----------------|
| Google Site Kit | Active plugin, GTM dns-prefetch on live site | **Replace** — env-based GA4/GTM |
| Google Analytics (GA4) | References in DB | **Migrate** (new property or same ID via env var) |
| Facebook Pixel | 3 references in DB | **Needs review** — confirm if still active |
| Jetpack | Active plugin | **Obsolete** |
| LinkedIn Insight Tag | Not confirmed in DB scan | **Needs review** |

**Do not copy tracking IDs into public documentation.** Configure via environment variables.

---

## Redirects

| Source | Count | Classification |
|--------|-------|----------------|
| Redirection plugin (`wp_redirection_items`) | Rules in DB | **Migrate** → Redirects collection |
| Pretty Links (`wp_prli_*`) | Tables in DB | **Migrate** — short links |
| WordPress native | Unknown | **Needs review** |

---

## SEO logic

| Source | Classification |
|--------|----------------|
| Yoast SEO (`wp_yoast_*` tables) | **Migrate** — see seo-migration.md |
| Astra theme meta | **Replace** |
| Elementor page settings | **Migrate** (title/description where set) |

---

## Hard-coded URLs

Navigation menu items contain hard-coded production URLs:

- `https://onixdatacentres.com/o-home/o-ghana/our-solutions/#tangor` (legacy path structure)
- Anchor links: `#virtualmachines`, `#peering`, `#cybersecurity`, etc.
- Broken: `http://French` in Language menu

**Classification:** **Migrate** — rebuild with relative paths and structured service anchors

---

## Custom fonts

| Font | Source |
|------|--------|
| Roboto | Elementor kit (Google Fonts) |
| Roboto Slab | Elementor kit |
| Poppins | Elementor kit custom typography |
| Inter | CFO ROI Calculator |
| Space Grotesk | CFO ROI Calculator |
| System sans-serif | Astra fallback |

**Classification:** **Migrate** — self-host or continue Google Fonts with privacy-compliant loading

---

## Custom menu behaviour

- Sticky header with scroll effects (custom CSS)
- Multi-menu setup: Ghana, Senegal, Language switcher
- Mobile hamburger via Astra + Elementor nav-menu
- Anchor-based service sub-navigation

**Classification:** **Migrate** → Header Navigation global in Payload

---

## Feature classification summary

| Feature | Action |
|---------|--------|
| Astra theme | Obsolete |
| Elementor page builder | Obsolete (rebuild as components) |
| Elementor templates/sections | Replace with React components |
| CFO ROI Calculator | Migrate |
| Sticky header behaviour | Migrate |
| Contact forms (all plugins) | Replace |
| Yoast SEO data | Migrate (from DB) |
| Redirection rules | Migrate |
| Pretty Links | Migrate |
| Polylang / TranslatePress | Needs review (FR content scope) |
| Cookie consent (Complianz/CKY) | Replace |
| Google Site Kit / GA4 | Replace (env-configured) |
| Jetpack | Obsolete |
| Floating/sticky CTA buttons | Replace |
| Leadership profile pages | Replace (Leadership collection) |
| RevSlider tables in DB | Obsolete (verify not in use) |
| Wordfence tables in DB | Obsolete |
